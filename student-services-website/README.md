# Student Services Website

Scaffolded Vite + React + TypeScript project with Tailwind CSS.

Quick start (PowerShell):

```powershell
cd "c:/System Analysis/Vibecoding After Patterns/student-services-website"
npm install
npm run dev
```

Tailwind is configured — if you need to reinitialize run `npx tailwindcss init -p`.
